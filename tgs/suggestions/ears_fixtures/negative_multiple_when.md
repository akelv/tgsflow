# Negative — Multiple "when" triggers (cardinality)
When a request arrives, when the cache is cold, the API shall recompute results.

When the user logs in and when MFA fails, the Auth Service shall alert.
